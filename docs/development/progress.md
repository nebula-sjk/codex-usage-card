# Progress

## 2026-07-14

- 已读取 computer-use 与 planning-with-files 技能规范。
- 已确认当前卡片源代码、应用包和本地日志数据源。
- 已完成 Swift UI 与用量字段解析修改。
- 已完成 Swift 重写：移除 5 小时窗口，识别 10080 分钟周窗口，增加官网样式的重置行、默认缩小尺寸和右下角拖拽缩放。
- 已编译通过；已重启常驻任务并完成桌面视觉检查。
- 已验证常驻任务 `local.codex.usage-card` 处于 running 状态，当前进程使用新版本卡片。
- 已修复一次启动时标签引用未绑定导致的退出问题。
- 已完成交互优化：使用限额整行改为原生按钮命中区，加入 hover、pressed、键盘焦点和辅助功能标签；关闭按钮扩展为 44×44 命中区；修正下拉箭头方向。
- 已完成优化版编译和桌面视觉验证；常驻任务已重新注册为 `running`。
- 模拟点击时触发了 macOS System Events 权限提示，未授予该权限；未修改系统隐私设置。
- 已按参考图修正进度条语义：绿色填充比例改为剩余百分比，并加入对应的辅助功能描述。
- 已验证当前卡片显示绿色约 76% 与“剩余 76%”一致，常驻任务状态为 `running`。
- 已修复额度读取：从最近 400 条日志改为筛选真实额度键值记录，避免普通请求和聊天文本挤出额度响应；已验证卡片恢复显示“剩余 99%”，常驻任务状态为 `running`。
- 已完成浅色柔和渐变风格改造：加入状态渐变背景、大号剩余百分比、轻边框、柔和行容器和浅色文字层级；保留 7 天额度、绿色剩余进度条、重置行和窗口缩放。
- 已验证新风格在桌面常驻运行，当前显示“96%”且数据正常读取。
- 已修复文字排版：底部内容改为稳定的自下而上布局，避免窗口边框裁切；重置胶囊文字改用字体基线计算，已验证“可用 1 次”垂直居中。
- 已将排版修正版注册为常驻任务，状态为 `running`。
- 已将重置行改为“限额重置次数（仅显示作用）”，明确不可直接重置，并增加“重置次数过期：日期”信息。
- 已让标题根据日志中的套餐字段动态显示，例如 `CODEX · PLUS`，不再固定为 PRO。
- 已新增 macOS 风格红色关闭按钮，并保留更大的点击命中区。
- 已新增收起交互：静止时显示 60×60 漂浮小球，进入小球区域后展开，离开约 0.45 秒后收起；加入轻量鼠标轮询兼容无边框非激活窗口。
- 已兼容多屏定位：优先将卡片放到启动时鼠标所在屏幕的右上角；最终版本编译通过并注册为常驻任务。

## 2026-07-15

- 已将收起小球调整为 50×50，并在球心显示剩余额度百分比。
- 已将离开收起延迟缩短为 0.12 秒，收起过程改为即时完成；展开仍保留短动画以维持空间连续性。
- 已修正重置次数胶囊文字的垂直基线计算，正确处理 macOS 字体的负 descender，避免文字上下偏移。
- 已重新编译并注册常驻任务，状态为 `running`；双屏截图确认小球显示为百分比。
- 已完成界面复审：补充小球/卡片 VoiceOver 语义标签，为可点击重置行和关闭按钮增加指针反馈；最终版本编译通过并恢复为 `running`。
- 已将悬停展开改为 0.28 秒延迟，并在按住拖动期间暂停展开，避免拖动小球时误触发放大。
- 已将卡片外框与内部行边框调整为白色半透明描边。
- 已加入额度三档状态：健康（60–100%）、谨慎（30–59%）、紧急（0–29%），分别使用绿色、琥珀色和红色强调。
- 已将重置卡片过期时间与周窗口重置时间拆分，支持从卡片专用字段中取最近过期时间；当前日志没有返回该字段时显示“暂未读取”。
- 本轮仅调整悬停展开为 0.50 秒、隐藏右上角关闭按钮，并扩展重置卡片截止时间字段候选；其他界面保持不变。
- 已修复展开时统一取消隐藏导致红叉再次出现的问题；关闭按钮在收起与展开状态都会保持隐藏。
- 重置卡片截止时间继续以服务端字段优先；当前响应头缺失该字段时，使用官网面板最近确认的 2026-08-13 作为到期前兜底。
- 已将额度三级状态文案调整为“充足 / 适中 / 告急”，阈值和配色保持不变。
- 已将浮球同步到三级状态：沿用卡片的绿色、琥珀色、红色语义色环，并增加小状态点；VoiceOver 标签同步包含状态名称。
- 已将展开卡片外框同步为浮球的三级状态色环，使用较低透明度保持桌面融合感；布局与内容不变。
- 已加入 Codex 窗口跟随：通过 macOS 桌面窗口列表检测 Codex/Codex++ 可见窗口；窗口不在桌面时自动隐藏浮球，窗口出现时自动恢复显示，每 0.5 秒检查一次。
- 已修正宿主窗口识别：当前 Codex 任务运行在 ChatGPT 桌面应用中，实际可见窗口所属名称为 ChatGPT；检测规则已兼容 ChatGPT、Codex 与 Codex++。
